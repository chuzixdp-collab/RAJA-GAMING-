import { ContactView } from "./contact-view";

export const dynamic = "force-dynamic";

export const metadata = { title: "Contact Messages | RAJA GAMING Admin" };

export default function AdminContactPage() {
  return <ContactView />;
}
